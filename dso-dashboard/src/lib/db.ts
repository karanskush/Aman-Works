// Prisma client for local development (used by prisma/ scripts, not by the app).
// The app uses pre-computed KPI data from kpi-registry.ts for Vercel compatibility.
// To use: import from prisma/compute-advanced-kpis.ts directly with ts-node.
export {};
